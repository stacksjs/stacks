import { type AppConfig, type BinaryConfig, type CacheConfig, type CdnConfig, type ChatConfig, type CliConfig, type DatabaseConfig, type DependenciesConfig, type DnsConfig, type EmailConfig, type Events, type GitConfig, type HashingConfig, type JobConfig, type LibraryConfig, type Model, type NotificationConfig, type PaymentConfig, type QueueConfig, type SearchEngineConfig, type ServicesConfig, type SmsConfig, type StacksConfig, type StorageConfig, type UiConfig } from '@stacksjs/types'

export function defineStacksConfig(config: Partial<StacksConfig>) {
  return config
}

export function defineApp(config: Partial<AppConfig>) {
  return config
}

export function defineBinary(config: Partial<BinaryConfig>) {
  return config
}

export function defineCache(config: Partial<CacheConfig>) {
  return config
}

export function defineCdn(config: Partial<CdnConfig>) {
  return config
}

export function defineChat(config: Partial<ChatConfig>) {
  return config
}

export function defineCli(config: Partial<CliConfig>) {
  return config
}

export function defineJob(config: Partial<JobConfig>) {
  return config
}

export function defineCronJob(config: Partial<JobConfig>) {
  return config
}

export function defineDatabase(config: Partial<DatabaseConfig>) {
  return config
}

export function defineDependencies(config: Partial<DependenciesConfig>) {
  return config
}

export function defineDns(config: Partial<DnsConfig>) {
  return config
}

export function defineEmailConfig(config: Partial<EmailConfig>) {
  return config
}

export function defineEmail(config: Partial<EmailConfig>) {
  return config
}

export function defineGit(config: Partial<GitConfig>) {
  return config
}

export function defineHashing(config: Partial<HashingConfig>) {
  return config
}

export function defineLibrary(config: Partial<LibraryConfig>) {
  return config
}

export function defineNotification(config: Partial<NotificationConfig>) {
  return config
}

export function definePayment(config: Partial<PaymentConfig>) {
  return config
}

export function defineQueue(config: Partial<QueueConfig>) {
  return config
}

export function defineSearchEngine(config: Partial<SearchEngineConfig>) {
  return config
}

export function defineServices(config: Partial<ServicesConfig>) {
  return config
}

export function defineSms(config: Partial<SmsConfig>) {
  return config
}

export function defineStorage(config: Partial<StorageConfig>) {
  return config
}

export function defineUi(config: Partial<UiConfig>) {
  return config
}

export function defineModel(config: Partial<Model>) {
  return config
}

export function defineEvents(config: Partial<Events>) {
  return config
}
