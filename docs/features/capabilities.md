# Driver capabilities

Stacks lets you swap the driver behind most subsystems - the database, the queue, the cache,
storage, mail, realtime and deploy. This page says which of those are actually supported, how
far, and what evidence backs the claim.

It is not a marketing table. `capabilityRegistry` in `@stacksjs/config` is the **allowlist**:
configuring a driver that is not in it fails config validation with the known names, and
configuring one marked `unsupported` fails with the reason. So a driver missing from this page
is a driver you cannot select.

<!-- capability-matrix:begin -->

<!-- Generated by `buddy docs:capabilities` from `@stacksjs/config`'s
     `capabilityRegistry`. Edit that, not this. -->

- **supported** - Contract evidence runs in CI.
- **partial** - Works, with the gaps named below.
- **experimental** - Usable, but without retained evidence.
- **unsupported** - Cannot be selected; configuring it fails loudly.

### Database

| Driver | Status | Topology | Live service | Evidence |
|---|---|---|---|---|
| `sqlite` | **supported** | embedded | - | `integration.test.ts`<br>`sqlite-tx-serialization.test.ts` |
| `mysql` | **partial** | client-server | - | `integration.test.ts` |
| `singlestore` | **experimental** | client-server | - | none retained |
| `vitess` | **experimental** | mysql-behind-vtgate | - | `vitess-constraints.test.ts` |
| `postgres` | **partial** | client-server | - | `postgres-pivot-timestamptz.test.ts` |
| `dynamodb` | **unsupported** | managed-service | - | none retained |

**Limitations**

- **`mysql`** - The default CI suite does not yet publish a dedicated dialect report.
- **`mysql`** - The migration files shipped under database/migrations are emitted for a single dialect (SQLite in the default template) and do not replay on MySQL. They must be regenerated against MySQL before migrating.
- **`singlestore`** - MySQL wire compatibility exists, but a dedicated conformance matrix is not published.
- **`vitess`** - No live cluster in CI; the retained evidence covers unsharded and sharded DDL profiles plus VSchema generation, not a round trip through vtgate.
- **`vitess`** - Set DB_VITESS_SHARDED=true for a sharded keyspace; those keyspaces reject cross-shard foreign keys and AUTO_INCREMENT, so use application-generated IDs and a VSchema.
- **`vitess`** - Schema changes on sharded keyspaces should go through Vitess online DDL.
- **`vitess`** - A transaction spanning shards is best-effort or two-phase, not the single-node ACID the ORM assumes.
- **`postgres`** - The default CI suite does not provision PostgreSQL.
- **`postgres`** - The migration files shipped under database/migrations are emitted for a single dialect (SQLite in the default template) and do not replay on PostgreSQL. They must be regenerated against PostgreSQL before migrating.
- **`dynamodb`** - DynamoDB helpers are not an ORM SQL driver and cannot be selected as database.default.

### Queue

| Driver | Status | Topology | Live service | Evidence |
|---|---|---|---|---|
| `sync` | **supported** | inline | - | `jobs.test.ts` |
| `database` | **supported** | worker-and-embedded-sqlite | - | `integration.test.ts`<br>`failure-path-correctness.test.ts` |
| `redis` | **supported** | worker-and-redis | redis 8.8.0 | `redis-contract.test.ts` |
| `sqs` | **unsupported** | managed-service | - | none retained |
| `memory` | **unsupported** | in-process | - | none retained |
| `beanstalkd` | **unsupported** | client-server | - | none retained |

**Limitations**

- **`sync`** - Not durable.
- **`database`** - Other SQL providers require their own provider evidence.
- **`redis`** - The retained contract covers queue persistence operations; horizontal scaling and distributed-lock extensions require separate evidence.
- **`sqs`** - Configuration shape is reserved; dispatch fails loudly.
- **`memory`** - Reserved driver name; dispatch fails loudly.
- **`beanstalkd`** - Reserved driver name; dispatch fails loudly.

### Cache

| Driver | Status | Topology | Live service | Evidence |
|---|---|---|---|---|
| `memory` | **supported** | in-process | - | `Memory.test.ts`<br>`ttl.test.ts` |
| `redis` | **supported** | client-server | redis 8.8.0 | `redis-contract.test.ts` |
| `singlestore` | **experimental** | client-server | - | none retained |

**Limitations**

- **`memory`** - Not shared across processes.
- **`redis`** - The retained contract covers core cache and TTL operations; TLS/authenticated deployments require separate provider evidence.
- **`singlestore`** - No dedicated CI service matrix.

### Storage

| Driver | Status | Topology | Live service | Evidence |
|---|---|---|---|---|
| `local` | **supported** | local-filesystem | - | `localStorage.test.ts`<br>`deep-integration.test.ts` |
| `memory` | **supported** | in-process | - | `memoryAdapter.test.ts` |
| `s3` | **partial** | managed-object-storage | - | `s3-presigned-post.test.ts` |
| `bun` | **partial** | local-filesystem | - | `bunAdapter.test.ts` |
| `azure` | **partial** | managed-object-storage | - | `azure-adapter.test.ts`<br>`azure-signing.test.ts` |

**Limitations**

- **`memory`** - Not persistent.
- **`s3`** - CI covers request construction but not a live provider round trip.
- **`bun`** - Adapter-specific feature parity is not fully reported.
- **`azure`** - CI covers Shared Key and service-SAS signing plus request construction, not a live container round trip.
- **`azure`** - Visibility is a container property on Azure, so changeVisibility() refuses rather than silently doing nothing; a per-object grant is signedUrl().
- **`azure`** - ensureConfiguredBuckets provisions S3 buckets only, so the container must exist before the disk is used.

### Mail

| Driver | Status | Topology | Live service | Evidence |
|---|---|---|---|---|
| `log` | **supported** | local-file | - | `email.test.ts` |
| `capture` | **supported** | in-process | - | `email.test.ts` |
| `smtp` | **partial** | smtp-server | - | `driver-credentials.test.ts` |
| `ses` | **partial** | managed-service | - | `driver-credentials.test.ts` |
| `sendgrid` | **partial** | managed-service | - | `driver-credentials.test.ts` |
| `mailgun` | **partial** | managed-service | - | `driver-credentials.test.ts` |
| `mailtrap` | **partial** | managed-service | - | `driver-credentials.test.ts` |

**Limitations**

- **`log`** - Development/testing transport only.
- **`capture`** - Testing transport only.
- **`smtp`** - CI validates configuration and request behavior without a live delivery assertion.
- **`ses`** - CI validates configuration and request behavior without a live delivery assertion.
- **`sendgrid`** - CI validates configuration and request behavior without a live delivery assertion.
- **`mailgun`** - CI validates configuration and request behavior without a live delivery assertion.
- **`mailtrap`** - CI validates configuration and request behavior without a live delivery assertion.

### Realtime

| Driver | Status | Topology | Live service | Evidence |
|---|---|---|---|---|
| `websocket` | **supported** | single-process-websocket | - | `ws-auth.test.ts`<br>`backpressure.test.ts` |

**Limitations**

- **`websocket`** - Scale-out requires an application-provided fan-out layer.

### Deploy

| Driver | Status | Topology | Live service | Evidence |
|---|---|---|---|---|
| `ts-cloud-hetzner` | **experimental** | hetzner-vm-systemd-rpx | - | `deploy-ssh-target.test.ts` |
| `ts-cloud-ssh` | **experimental** | ssh-box-systemd-rpx | - | `deploy-ssh-target.test.ts` |

**Limitations**

- **`ts-cloud-hetzner`** - Live provider evidence and rollback conformance are not yet retained in the protocol report.
- **`ts-cloud-ssh`** - DNS, TLS issuance, CDN and mail reconciliation are skipped for a host on a private address.
- **`ts-cloud-ssh`** - One app host per project; multi-host ssh fleets are not supported.
- **`ts-cloud-ssh`** - No live hardware evidence is retained in CI.

<!-- capability-matrix:end -->

## Reading it

**Status** is a judgement about evidence, not about code quality. `supported` means a contract
test runs in CI. `partial` means it works and something is not covered - the limitation says
what. `experimental` means it is usable but nothing is retained to prove it stays that way.

**Evidence** names the test files behind the status. They are checked to exist: a driver called
supported on the strength of a test that has been deleted is the failure this guards against
(`capabilities.test.ts`).

**Live service** is filled in when CI runs against a real instance of the service rather than a
mock, and names the version it ran against.

A missing round trip against a real provider is the most common reason for `partial`. It usually
means request construction and signing are covered but nobody has pointed CI at a live account -
often because that needs credentials somebody has to own.

## Using it from code

```ts
import { assertCapabilityAvailable, capabilityDrivers, findCapability } from '@stacksjs/config'

// Throws for an unknown or unsupported driver, naming the alternatives.
assertCapabilityAvailable('queue', 'redis')

// Everything selectable in a category.
capabilityDrivers('storage').map(driver => driver.name)

// The whole record, including limitations and evidence.
findCapability('storage', 'azure')
```

The table above is generated from that registry by `buddy docs:capabilities`, and CI fails when
the two disagree. Change the registry, not this page.

## Related

- [Desktop support matrix](../packages/desktop.md) - the same treatment for platform targets
- `stacks-database`, `stacks-queue`, `stacks-cache`, `stacks-storage`, `stacks-email` for how to
  configure each
