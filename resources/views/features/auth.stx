<script server>
// Auth. The page itself is resources/partials/feature-page.stx, shared
// by all eight; its content is the 'auth' entry in
// resources/data/features.ts. This file exists so that /features/auth is
// a real route and anything else under /features is a real 404 - see the note
// in the partial.
const featureSlug = 'auth'
</script>

@include('feature-page')
