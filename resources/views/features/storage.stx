<script server>
// Storage. The page itself is resources/partials/feature-page.stx, shared
// by all eight; its content is the 'storage' entry in
// resources/data/features.ts. This file exists so that /features/storage is
// a real route and anything else under /features is a real 404 - see the note
// in the partial.
const featureSlug = 'storage'
</script>

@include('feature-page')
