<script server>
// CMS. The page itself is resources/partials/feature-page.stx, shared
// by all eight; its content is the 'cms' entry in
// resources/data/features.ts. This file exists so that /features/cms is
// a real route and anything else under /features is a real 404 - see the note
// in the partial.
const featureSlug = 'cms'
</script>

@include('feature-page')
