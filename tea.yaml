# based on your config, Stacks will determine the project's dependencies
# updates will be applied to this file
dependencies:
  nodejs.org: ^18.16.1
  pnpm.io: ^8.6.4
  sqlite.org: ^3.42.0
  # mysql.org: ^8.0.33
