# based on your config, Stacks will determine the project's dependencies
# updates will be applied to this file
dependencies:
  nodejs.org: ^18.16.1
  npmjs.com: ^9.7.2 # needed for npm adduser
  pnpm.io: ^8.6.5
  sqlite.org: ^3.42.0
  redis.io: ^7.0.11
  # mysql.org: ^8.0.33
