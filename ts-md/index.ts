// ts-md stub - Markdown parsing utilities
export function parse(markdown: string): string {
  return markdown
}
export function render(markdown: string): string {
  return markdown
}
export default { parse, render }
