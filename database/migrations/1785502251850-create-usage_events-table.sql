-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS "usage_events" (
  "id" INTEGER PRIMARY KEY AUTOINCREMENT,
  "meter" TEXT CHECK ("meter" IN ('contacts', 'email_sends', 'sms_segments', 'ai_generations', 'storage_bytes')) not null,
  "quantity" INTEGER not null default 1,
  "idempotency_key" TEXT not null,
  "metadata" TEXT,
  "occurred_at" TEXT not null,
  "team_id" INTEGER REFERENCES "teams"("id"),
  "created_at" TEXT not null default CURRENT_TIMESTAMP,
  "updated_at" TEXT,
  "uuid" TEXT
);
CREATE UNIQUE INDEX IF NOT EXISTS "usage_events_idempotency_unique" ON "usage_events" ("idempotency_key");
CREATE INDEX IF NOT EXISTS "usage_events_meter_period" ON "usage_events" ("team_id", "meter", "occurred_at");
CREATE UNIQUE INDEX IF NOT EXISTS "usage_events_uuid_unique" ON "usage_events" ("uuid");
