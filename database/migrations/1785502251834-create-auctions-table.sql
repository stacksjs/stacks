-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS "auctions" (
  "id" INTEGER PRIMARY KEY AUTOINCREMENT,
  "event_id" INTEGER,
  "title" TEXT not null,
  "description" TEXT,
  "status" TEXT CHECK ("status" IN ('draft', 'preview', 'open', 'closed', 'settled')) not null default 'draft',
  "currency" TEXT default 'USD',
  "goal_amount" INTEGER,
  "opens_at" TEXT not null,
  "closes_at" TEXT not null,
  "anti_snipe_minutes" INTEGER default 2,
  "extend_on_bid_window_minutes" INTEGER default 2,
  "max_extensions" INTEGER default 20,
  "created_at" TEXT not null default CURRENT_TIMESTAMP,
  "updated_at" TEXT,
  "uuid" TEXT
);
CREATE INDEX IF NOT EXISTS "auctions_event_id_index" ON "auctions" ("event_id");
CREATE INDEX IF NOT EXISTS "auctions_status_index" ON "auctions" ("status");
CREATE UNIQUE INDEX IF NOT EXISTS "auctions_uuid_unique" ON "auctions" ("uuid");
