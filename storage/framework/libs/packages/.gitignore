# Generated by `buddy build:libs` / `buddy generate:entries` from
# config/library.ts. Every package here is staged sources + a generated
# manifest + build output, so it is rebuilt rather than committed.
*
!.gitignore
