<script setup lang="ts">
import { VStepper } from '@vue-stepper-component'

interface Props {
    steps: number, //used as number of steps on your stepper
    step: number // current step
}

const {
  steps,
  step
} = defineProps<Props>()
</script>

<template>
   <v-stepper :steps="steps" v-model="step"></v-stepper>
</template>
