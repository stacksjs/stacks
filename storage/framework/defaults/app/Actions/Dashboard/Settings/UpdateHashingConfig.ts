import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateHashingConfig',
  description: 'Updates the hashing config.',
  apiResponse: true,

  async handle() {
    //
  },
})
