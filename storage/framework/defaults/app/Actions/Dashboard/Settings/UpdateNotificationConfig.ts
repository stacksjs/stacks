import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateNotificationConfig',
  description: 'Updates the notification config.',
  apiResponse: true,

  async handle() {
    //
  },
})
