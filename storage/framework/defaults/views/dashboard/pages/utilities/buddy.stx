@extends('layouts/default')

@section('content')
<script server>
let commands = []
let recentOutput = ''
let stats = []

function statusVariant(s) {
  s = String(s || '').toLowerCase()
  if (['success', 'completed'].includes(s)) return 'success'
  if (['running', 'active'].includes(s)) return 'primary'
  if (['pending', 'waiting'].includes(s)) return 'warning'
  if (['failed', 'error'].includes(s)) return 'danger'
  return 'default'
}
</script>
<div class="p-6 space-y-6">

  <PageHeader title="Buddy CLI" description="Command line interface for Stacks">
    <template #actions>
      <Button variant="secondary">View Docs</Button>
      <Button variant="primary">Open Terminal</Button>
    </template>
  </PageHeader>

  <!-- Stats -->
  <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
    @foreach (stats as stat)
      <StatsCard :title="stat.label" value="{{ stat.value }}" icon="i-hugeicons-terminal" iconBg="primary" />
    @endforeach
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
    <!-- Quick Commands -->
    <div class="bg-white dark:bg-neutral-800/90 rounded-xl border border-black/5 dark:border-white/5">
      <div class="p-4 border-b border-neutral-200/60 dark:border-white/5">
        <span class="text-[14px] font-semibold text-neutral-900 dark:text-white">Quick Commands</span>
      </div>

      <DataTable
        :columns="{{ JSON.stringify([
          { key: 'command', label: 'Command' },
          { key: 'description', label: 'Description' },
          { key: 'lastRun', label: 'Last Run' },
          { key: 'status', label: 'Status' },
          { key: 'actions', label: '' }
        ]) }}"
        :data="commands"
        emptyTitle="No commands"
        emptyDescription="Available commands will appear here"
        emptyIcon="i-hugeicons-terminal"
      >
        @foreach (commands as cmd)
          <tr class="hover:bg-neutral-50 dark:hover:bg-white/[0.02] transition-colors">
            <td class="px-4 py-3">
              <code class="text-xs font-mono px-1.5 py-0.5 bg-neutral-100 dark:bg-neutral-700 rounded text-neutral-700 dark:text-neutral-300">{{ cmd.command }}</code>
            </td>
            <td class="px-4 py-3 text-[12px] text-neutral-500 dark:text-neutral-400">{{ cmd.description }}</td>
            <td class="px-4 py-3 text-[12px] text-neutral-500 dark:text-neutral-400">{{ cmd.lastRun }}</td>
            <td class="px-4 py-3">
              <Badge variant="{{ statusVariant(cmd.status) }}" size="xs" dot>{{ cmd.status }}</Badge>
            </td>
            <td class="px-4 py-3">
              <Button variant="outline" size="xs">Run</Button>
            </td>
          </tr>
        @endforeach
      </DataTable>
    </div>

    <!-- Recent Output -->
    <Card>
      <div class="p-4 border-b border-neutral-200/60 dark:border-white/5 flex items-center justify-between">
        <span class="text-[14px] font-semibold text-neutral-900 dark:text-white">Recent Output</span>
        <Button variant="ghost" size="xs">Clear</Button>
      </div>
      <div class="p-4">
        <div class="bg-[#1a1a2e] text-green-400 p-4 rounded-lg font-mono text-[13px] overflow-auto max-h-[300px]">
          <pre class="m-0 whitespace-pre-wrap">{{ recentOutput }}</pre>
        </div>
      </div>
    </Card>
  </div>

  <!-- Run Custom Command -->
  <Card>
    <div class="p-4 border-b border-neutral-200/60 dark:border-white/5">
      <span class="text-[14px] font-semibold text-neutral-900 dark:text-white">Run Custom Command</span>
    </div>
    <div class="p-4">
      <div class="flex gap-2">
        <input type="text" class="flex-1 px-3 py-2 text-sm font-mono border border-neutral-200 dark:border-neutral-700 rounded-lg bg-white dark:bg-neutral-800 text-neutral-900 dark:text-white focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="buddy ..." />
        <Button variant="primary">Execute</Button>
      </div>
    </div>
  </Card>
</div>
@endsection
