import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdatePaymentConfig',
  description: 'Updates the payment config.',
  apiResponse: true,

  async handle() {
    //
  },
})
