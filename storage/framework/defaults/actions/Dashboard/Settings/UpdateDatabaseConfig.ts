import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateDatabaseConfig',
  description: 'Updates the database config.',
  apiResponse: true,

  async handle() {
    //
  },
})
