import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateCloudConfig',
  description: 'Updates the cloud config.',
  apiResponse: true,

  async handle() {
    //
  },
})
