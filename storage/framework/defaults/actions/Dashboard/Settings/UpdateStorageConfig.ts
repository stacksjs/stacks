import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateStorageConfig',
  description: 'Updates the storage config.',
  apiResponse: true,

  async handle() {
    //
  },
})
