import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateSearchEngineConfig',
  description: 'Updates the search engine config.',
  apiResponse: true,

  async handle() {
    //
  },
})
