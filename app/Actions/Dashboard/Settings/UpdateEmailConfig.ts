import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateEmailConfig',
  description: 'Updates the email config.',
  apiResponse: true,

  async handle() {
    //
  },
})
