import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateLibraryConfig',
  description: 'Updates the library config.',
  apiResponse: true,

  async handle() {
    //
  },
})
