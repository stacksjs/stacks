import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateSecurityConfig',
  description: 'Updates the security config.',
  apiResponse: true,

  async handle() {
    //
  },
})
