import { Action } from '@stacksjs/actions'

export default new Action({
  name: 'UpdateServicesConfig',
  description: 'Updates the services config.',
  apiResponse: true,

  async handle() {
    //
  },
})
